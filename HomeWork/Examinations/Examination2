#include <stdio.h>
#include <stdlib.h>

int grade (int a, int n)
{
if (n==0) {
return 1;
}
if(n>1) {
return grade(a, n-1)*a;
}
else {
return a;
}
}

int main()
{
int a, n;
scanf("%d, %d", &a, &n);
printf("%d", grade(a,n));
return 0;
}
