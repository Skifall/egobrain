#include <stdio.h>
#include <stdlib.h>

int fibb (int k){
    if (k==1) return 1;
    if (k==0) return 1;
    if (k>1) return (fibb(k-1)+fibb(k-2));
}
int main()
{
    int n;
    scanf("%d",&n);
    printf("%d", fibb(n));
    return 0;
}
