#include <stdio.h>
#include <stdlib.h>

int main()
{
srand( time( 0 ) );
int array[17];
int mynull = 0;
int i;
    for(i = 0; i<17; i++){
    array[i] = rand() % 4 -2;
    printf("%d\n", array[i]);
    if (array[i]==0){
    mynull += 1;
    }
}
printf("\n%d", mynull);
return 0;
}
