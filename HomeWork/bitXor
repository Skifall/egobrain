#include <stdio.h>
#include <stdlib.h>

int main()
{
    int variable0,variable1;
    scanf("%d, %d", &variable0, &variable1);
    printf("%d", ~(~(~variable0&variable1)&~(variable0&~variable1)));
    return 0;
}
